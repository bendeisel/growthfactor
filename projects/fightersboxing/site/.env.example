# The schedule in Sanity. Copy this file to .env and fill in the project id.
# Both are PUBLIC on purpose: the browser re-checks the schedule too, and the
# dataset is read through Sanity's read-only public CDN with no token.
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
